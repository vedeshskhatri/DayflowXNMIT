// Phase 1 placeholder — confirms the Vite container boots and React renders.
// Replaced in Phase 5 with real routing (SignIn, ResetPassword, Dashboard).
export default function App() {
  return (
    <div className="flex min-h-screen items-center justify-center bg-gray-50">
      <div className="text-center">
        <h1 className="text-4xl font-bold text-gray-800">Dayflow</h1>
        <p className="mt-2 text-gray-500">Every workday, perfectly aligned.</p>
      </div>
    </div>
  );
}
